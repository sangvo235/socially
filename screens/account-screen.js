import { View, Text, ScrollView } from 'react-native';

const AccountScreen = () => {

    return (
    <ScrollView>
        <Text className="text-3xl pt-12 pb-6 text-center font-semibold bg-white"> Account Profile
        </Text>
    </ScrollView>

    );

}

export default AccountScreen;